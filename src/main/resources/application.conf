ktor {
    deployment {
        port = 8080
        port = ${?PORT}

        #TODO - sslPort
        #sslPort: 8443

        host = 192.168.0.50
        host = localhost
        host = ${?HOST}
    }
    application {
        modules = [ com.example.ApplicationKt.module ]
    }
}