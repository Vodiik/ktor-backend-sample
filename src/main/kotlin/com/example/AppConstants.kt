package com.example

object AppConstants {
    private const val LOCALHOST_ORIGIN = "localhost:8080"
    const val DEPLOY_ORIGIN = "localhost"
    const val DEPLOY_PORT = 8080

    const val ORIGIN_URL = LOCALHOST_ORIGIN
}